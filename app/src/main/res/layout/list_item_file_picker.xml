<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:background="@drawable/row_selector_background"
                android:layout_height="match_parent"
                android:layout_width="match_parent">

    <ImageView
        android:id="@+id/icon"
        android:layout_alignParentStart="true"
        android:layout_height="@dimen/file_dialog_icon_height"
        android:layout_width="@dimen/file_dialog_icon_width"
        android:paddingEnd="@dimen/default_margin"
        android:paddingLeft="@dimen/default_margin"
        android:paddingRight="@dimen/default_margin"
        android:paddingStart="@dimen/default_margin"/>

    <TextView
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:id="@+id/file_name_input"
        android:layout_alignBottom="@+id/icon"
        android:layout_alignTop="@+id/icon"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/icon"
        android:layout_width="match_parent"
        android:text="@+id/file_name_input"/>

</RelativeLayout>